#include "holberton.h"

/**
 * _puts_recursion - check the code for Holberton School students.
 * @s: ssssss
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		_putchar(s[i]);
	}
}
