#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  char str1[15];
  char str2[15];

  strcpy(str1, "\"Programming is like building a multilingual puzzle"); 
  
  return (0);
}
